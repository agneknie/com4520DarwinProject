The python notebook provided provides the NLPAug functions that were used in the data augmentation.

There are many methods being called by default for all types of augmentation metods on different datasets. Comment/uncomment the ones you want to run.

You may need to upload the gold dataset seperately before training

in the training section you will need to comment/edit which file is used depending on augmentation type 


Any questions, please contact
